<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import alias="v" type="android.view.View"/>
        <import alias="text" type="android.text.TextUtils"/>
        <import alias="def" type="sgtmelon.scriptum.office.annot.def.ThemeDef"/>

        <variable name="noteItem" type="sgtmelon.scriptum.app.model.item.NoteItem"/>
        <variable name="listRoll"
                  type="java.util.List&lt;sgtmelon.scriptum.app.model.item.RollItem&gt;"/>

    </data>

    <androidx.cardview.widget.CardView
            style="@style/Card"
            app:cardCornerRadius="@dimen/radius_4dp"
            app:cardUseCompatPadding="false"
            app:noteColor='@{noteItem.color}'
            tools:cardBackgroundColor="?attr/clPrimary">

        <FrameLayout
                android:id="@+id/click_container"
                style="@style/NoteItem.Click">

            <LinearLayout style="@style/NoteItem.Container">

                <LinearLayout
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:orientation="vertical">

                    <TextView
                            style="@style/NoteItem.Name"
                            android:text='@{noteItem.name}'
                            android:visibility='@{text.isEmpty(noteItem.name) ? v.GONE : v.VISIBLE}'
                            tools:text="@string/test_note_name"/>

                    <include
                            layout="@layout/include_item_note_roll"
                            android:visibility='@{listRoll.size() &gt;= 1 ? v.VISIBLE : v.GONE}'
                            app:rollItem='@{listRoll.size() &gt;= 1 ? listRoll.get(0) : null}'/>

                    <include
                            layout="@layout/include_item_note_roll"
                            android:visibility='@{listRoll.size() &gt;= 2 ? v.VISIBLE : v.GONE}'
                            app:rollItem='@{listRoll.size() &gt;= 2 ? listRoll.get(1) : null}'/>

                    <include
                            layout="@layout/include_item_note_roll"
                            android:visibility='@{listRoll.size() &gt;= 3 ? v.VISIBLE : v.GONE}'
                            app:rollItem='@{listRoll.size() &gt;= 3 ? listRoll.get(2) : null}'/>

                    <include
                            layout="@layout/include_item_note_roll"
                            android:visibility='@{listRoll.size() &gt;= 4 ? v.VISIBLE : v.GONE}'
                            app:rollItem='@{listRoll.size() &gt;= 4 ? listRoll.get(3) : null}'/>

                    <include
                            layout="@layout/include_item_note_info"
                            app:noteItem='@{noteItem}'/>

                </LinearLayout>

                <ImageView
                        style="@style/NoteItem.Color.Vertical"
                        android:contentDescription="@null"
                        app:noteColor='@{noteItem.color}'
                        app:viewOnDark='@{true}'
                        app:visibleOn='@{def.dark}'
                        tools:tint="@color/note_green_dark"
                        tools:visibility="visible"/>

            </LinearLayout>

        </FrameLayout>

    </androidx.cardview.widget.CardView>

</layout>

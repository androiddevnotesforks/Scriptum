<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <import type="sgtmelon.handynotes.R" />

        <import type="sgtmelon.handynotes.office.Help.Draw" />

        <variable
            name="icon"
            type="int" />

        <variable
            name="title"
            type="int" />

        <variable
            name="details"
            type="int" />

    </data>

    <LinearLayout
        android:id="@+id/incPageInfo_ll_container"
        style="@style/Info.Container">

        <ImageView
            android:id="@+id/incPageInfo_iv_icon"
            style="@style/Info.Icon"
            android:contentDescription="@null"
            android:src='@{Draw.get(context, icon, R.attr.clSecond)}' />

        <TextView
            android:id="@+id/incPageInfo_tv_title"
            style="@style/Info.Title"
            android:text='@{context.getString(title)}' />

        <TextView
            android:id="@+id/incPageInfo_tv_details"
            style="@style/Info.Details"
            android:text='@{context.getString(details)}' />

    </LinearLayout>

</layout>